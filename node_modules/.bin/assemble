#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../semantic/bin/assemble" "$@"
  ret=$?
else 
  node  "$basedir/../semantic/bin/assemble" "$@"
  ret=$?
fi
exit $ret
